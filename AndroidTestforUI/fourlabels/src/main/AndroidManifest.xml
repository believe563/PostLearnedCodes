<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.fengjianghui.fourlabels" >

    <permission android:name="eee.fff.ggg"
        android:protectionLevel="normal"></permission>
    <application
        android:allowBackup="true"
    android:icon="@mipmap/ic_launcher"
    android:label="@string/app_name"
    android:theme="@style/AppTheme" >
    <activity
        android:permission="eee.fff.ggg"
        android:name=".MainActivity"
        android:label="@string/app_name" >
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />

            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
    </activity>
    <activity
        android:name=".MainActivity2"
        android:label="@string/title_activity_main_activity2" >
        <intent-filter>
            <action android:name="aaa.bbb.ccc"/>
            <category android:name="android.intent.category.DEFAULT"/>
        </intent-filter>
    </activity>

    <service android:name=".BaseService" ></service>

    <receiver android:name=".BaseReciever" ></receiver>

<provider
    android:authorities="aaa.bbb.ccc"
    android:name=".BaseProvider"></provider>


</application>

</manifest>
