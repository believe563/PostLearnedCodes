传统动画animation：系统会不断调用onShow()方法去重绘界面，只有位移旋转缩放透明度四种效果
                   使用传统动画只是改变了动画的位置，但如果给它加一个监听，仍然会在原来的地方实现响应，
                   所以animation不能用来做一些交互性的效果，只能用来做一些显示性的效果
                   重复调用onShow()方法显示界面必然会很耗费cpu资源，
属性动画animator：系统会直接调用get() set()方法去真实地改变一个属性